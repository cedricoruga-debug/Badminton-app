# Copy this file to .env.local and fill in your Supabase project's values
# (Project Settings -> API in the Supabase dashboard).

NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service role key — powers the /users (Accounts) admin page, which creates
# and deletes login accounts directly. Deliberately NOT prefixed with
# NEXT_PUBLIC_ so it's only ever readable on the server, never sent to the
# browser. Keep this one especially secret — it bypasses every RLS policy.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
